# Core Workflow Screenshots (item 613)

Store PNG (or JPEG) captures for Sprint 15 / university report evidence here.

## Naming

Use the **file name** from the catalog in
`frontend/src/features/testing/coreWorkflowScreenshots.ts` and
[Core Workflow Screenshots documentation](../../testing/core-workflow-screenshots.md).

Example:

```text
01-login-sign-in.png
02-dashboard-analytics.png
...
```

## Status

Binary screenshot files are **optional until manually captured**. Item **613** delivers the
inventory, capture procedure, and folder contract. Placing the image files is evidence work
and may be completed during demo prep or items **644–645**.

## Capture tips

1. Prefer the seeded demo admin: `admin@bayer-westphalian.test`.
2. Use desktop width ≥ 1281px unless capturing a responsive evidence shot.
3. Hide personal desktop chrome; include only the app window/browser content area.
4. After capture, keep file names stable so report appendices can link by path.
