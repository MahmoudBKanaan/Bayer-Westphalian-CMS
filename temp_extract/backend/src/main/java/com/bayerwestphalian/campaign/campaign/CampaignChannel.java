package com.bayerwestphalian.campaign.campaign;

/**
 * Campaign communication channel (KB {@code campaigns.channel} / {@code campaign_channel} enum).
 */
public enum CampaignChannel {
    EMAIL,
    PHONE,
    SMS,
    MIXED
}
