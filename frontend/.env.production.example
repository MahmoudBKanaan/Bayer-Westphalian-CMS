# Production frontend environment - Sprint 18 item 719.
# Vite variables are embedded in the browser bundle. Never place secrets here.

# Same-origin route expected to be forwarded to the backend by the production reverse proxy.
VITE_API_BASE_URL=/api
VITE_APP_ENV=prod
